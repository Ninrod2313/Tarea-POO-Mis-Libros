## Cambios realizados

Se agregó `LibroRepository` para administrar la colección de libros y se implementó el patrón Singleton para utilizar una única instancia del Repository.

La aplicación ahora utiliza la estructura:

App.tsx → LibroService → LibroRepository → Datos Mock